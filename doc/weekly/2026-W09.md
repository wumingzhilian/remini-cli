# Weekly Log 2026-W09

- Date anchor: 2026-02-25
- Topic: Spec 收敛与阶段里程碑
- Scope: remini-cli rewrite tracking
- Status: archived milestone note for historical weekly timeline

## Incremental Updates

- Added `--include-directories` parsing (comma-separated + repeated flags).
- Wired `@path` resolution to search `cwd` first, then included directories.
- Added tests for include-directory fallback and `cwd` precedence.
- Added `/commands` slash command and aligned output with `/help`.
- Added `--resume/-r` parsing with `--resume` defaulting to `latest` (stub flow).
- Added `/resume [session|latest]` slash command stub and help list entry.
- Added `--debug/-d` with env fallback (`DEBUG` / `DEBUG_MODE`) and startup debug notice.
- Added `/bug` slash command stub with issue-report guidance.
- Added `/directory` slash command stub with `list/add/remove` subcommands.
- Added `/copy` slash command stub with default target `last`.
- Added `/settings` slash command stub with `show/set` paths.
- Added `/compress` slash command stub with optional compression note.
- Added `/docs` slash command stub with project documentation entry link.
- Added `/init` slash command stub with workspace bootstrap guidance.
- Added `/privacy` slash command stub with security and telemetry guidance.
- Added `/editor` slash command stub with `status/open` flows.
- Added `/ide` slash command stub with `status/enable/disable` actions.
- Added `/theme` slash command stub with `list/set` actions.
- Added `/terminal-setup` slash command stub with `check/install` actions.
- Added `/vim` slash command stub with `status/on/off` actions.
