# Weekly Log 2026-W18

- Date anchor: 2026-04-29
- Topic: ecosystem slash commands
- Scope: remini-cli rewrite tracking
- Status: weekly rewrite increment

## Incremental Updates

- Added stubs for `/chat`, `/extensions`, `/hooks`, `/mcp`, `/memory`, `/policies`, `/restore`, `/rewind`, `/setup-github`, `/shells`, and `/skills`.
- Expanded `/commands` coverage for the newer Gemini command surface.
- Added unit tests for representative command outputs and errors.
